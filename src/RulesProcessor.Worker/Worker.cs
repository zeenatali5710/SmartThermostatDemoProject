// Intentionally empty. The default template Worker was replaced by the four processors
// under Processors/ (StateProcessor, RuleProcessor, AlertProcessor, AnalyticsProcessor).
// (File retained because deletion is not permitted in this environment.)
